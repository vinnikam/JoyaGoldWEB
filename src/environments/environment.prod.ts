const host = 'http://localhost:8863/';

export const environment = {
  production: true,
  urlTipoProducto: `${host}api/tipoproductos/`,
  urlMateriales: `${host}api/materiales/`,
  urlProducto: `${host}api/productos/`
};
